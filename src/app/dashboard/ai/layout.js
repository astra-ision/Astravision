export const metadata = {
  title: 'AI Services | Astravision Dashboard',
  description: 'Manage and monitor your AI services and models',
};

export default function AILayout({ children }) {
  return (
    <div className="space-y-6">
      {children}
    </div>
  );
} 